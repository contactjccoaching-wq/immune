
## 21:07:22 | corrected | domains=["code","strategy"] | score=15
Docker+OpenClaw+Guardian setup scan. Found: plaintext OAuth creds on disk (critical), OpenClaw port exposure risk (critical), CRLF in shell scripts, pgrep cross-container failure, missing backoff in guardian retries. Added 6 antibodies + 4 strategies. Key patterns: loopback binding, HTTP healthcheck, :ro mounts, claude-cli backend.

## 16:04:33 | corrected | domains=["code","strategy"] | score=0
OpenClaw WhatsApp deployment scan. Found: self-chat loop (critical), CLI context overflow 787K tokens (critical), ghost sessions, read_only vs WhatsApp creds conflict, WhatsApp 440 session conflicts, --bare breaks OAuth. Added 6 antibodies + 5 strategies. Key patterns: private group as report sink, compound sender+group validation, agent-context.md persistence, desktop folder mount, Windows schtasks for Docker cron.
